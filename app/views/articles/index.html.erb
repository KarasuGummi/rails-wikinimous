<div class="container">
  <h3 class="home-link">Wikinimous</h3>
  <p class="new-article">New Article</p>
</div>

<div class="container">
  <% @articles.each do |article| %>
    <div class="article">
      <h2><%= link_to article.title, article_path(article) %></h2>
      <%= article.content %>
    </div>
  <% end %>
</div>
